# WIP

```
Usage: aoc.ts [options]

Options:
  -d, --day <number>  specify the day as a number
  -n --new            create an empty day
  -f, --force         force creation even if files exist
  --test              run the tests
  -h, --help          output usage information
```
